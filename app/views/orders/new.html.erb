<%= form_for([@product, @order]) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="px-total">
  <div class="px-email" >
  <div class="field">
    <%= f.text_field :email, :placeholder => "email..." %>
  </div>
  <div class="field">
    <%= f.text_area :message, :placeholder => "mensagem...",
        :size => "300x10", :style => "width: 380px; height: 200px;" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
  <%= link_to "voltar", root_path, :class => "btn btn-small" %>
  </div>
  </div>
<% end %>
